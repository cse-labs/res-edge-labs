apiVersion: v1
kind: Service
metadata:
  name: rea.application.name
  namespace: rea.namespace.name
spec:
  selector:
    app: rea.application.name

  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
