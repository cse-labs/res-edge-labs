apiVersion: v1
kind: Service
metadata:
  name: tabs-spaces
  namespace: tabs-spaces
spec:
  selector:
    app: tabs-spaces

  type: NodePort
  ports:
    - port: 8080
      nodePort: 32088
      targetPort: http
      protocol: TCP
      name: http
