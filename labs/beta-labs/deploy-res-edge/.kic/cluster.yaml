###
# cluster commands
###

commands:
- name: cluster
  short: k3d cluster commands
  scriptCommands:
    - name: create
      short: Create a new local k3d cluster (deletes the existing cluster)
      path: cluster/create
    - name: delete
      short: Delete the k3d cluster
      path: cluster/delete
