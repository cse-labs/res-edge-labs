###
# check commands
###

commands:
- name: check
  short: Check local apps
  scriptCommands:
    - name: resedge
      short: Check ResEdge
      path: check/resedge
    - name: mssql
      short: Check mssql
      path: check/mssql
